package goat.projectLinearity.libs.gravedigging;

/**
 * Legacy Gravedigging subsystem placeholder. The original implementation has
 * been disabled to avoid runtime issues; this class now simply documents that
 * the feature is intentionally inactive.
 */
public final class Gravedigging {

    public static final String REFERENCE = "Gravedigging subsystem disabled; legacy code retained as text reference.";

    private Gravedigging() {
        // no-op placeholder
    }
}
